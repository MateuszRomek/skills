interface:
  display_name: "Setup Agent Mode"
  short_description: "Configure local model routing"
  default_prompt: "Use $setup-agent-mode to configure model routing for this host."
policy:
  allow_implicit_invocation: false
