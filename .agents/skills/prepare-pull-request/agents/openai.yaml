interface:
  display_name: "Prepare Pull Request"
  short_description: "Prepare a review-ready GitHub pull request"
  default_prompt: "Use $prepare-pull-request to publish the current work as a review-ready GitHub pull request."
policy:
  allow_implicit_invocation: true
